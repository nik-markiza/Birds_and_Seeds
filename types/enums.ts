export enum AuctionTabType {
  LIVE = 'Live',
  FEATURED = 'Featured',
  PAST = 'Past',
}

export enum TransmissionType {
  MANUAL = 'Manual',
  AUTOMATIC = 'Automatic',
}

export enum CarType {
  COUPE = 'coupe',
  CONVERTIBLE = 'convertible',
  HATCHBACK = 'hatchback',
  SEDAN = 'sedan',
  CROSSOVER = 'crossover',
  TRUCK = 'truck',
  VAN = 'van',
  WAGON = 'wagon',
}
